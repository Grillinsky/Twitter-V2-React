<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("../partials/meta") %>
  </head>
  <body id="logInBody">
    <div id="mainWrapper" class="d-flex align-items-center justify-content-center">
      <main class="bg-white rounded w-75">
        <div class="container-fluid">
          <div id="rowContainer" class="row">
            <!-- Aside for tablet & desktop -->
            <aside
              id="logInAside"
              class="d-none d-md-block d-lg-block col-md-6 col-lg-8 text-white rounded-start"
            >
              <i id="twitterIcon" class="fab fa-twitter fa-4x m-3 slide-in-right"
                ><span class="hide-element">Icono de Twitter</span></i
              >
              <div id="welcome" class="container m-4 px-4">
                <h2>Hey! Nice to see you again 🥰</h2>
              </div>
            </aside>
            <section
              class="col-12 col-md-6 col-lg-4 d-flex align-items-center justify-content-center"
            >
              <form method="post" class="w-100 p-3">
                <h1>Login</h1>
                <small>Ready to start using Twitter?</small>
                <div class="form my-3">
                  <input
                    type="text"
                    class="form-control mb-3"
                    id="userName"
                    name="username"
                    placeholder="Username or email"
                  />

                  <input
                    type="password"
                    class="form-control"
                    id="floatingPassword"
                    placeholder="Password"
                    name="password"
                  />
                </div>
                <div class="d-grid my-3">
                  <button id="logInButton" class="btn rounded-pill btn-fluid text-white mb-5">
                    Login
                  </button>
                </div>
                <% if(messages) {%>
                <small class="d-block text-center text-danger mb-3"><%= messages.info %></small>
                <% } %>

                <small class="d-block text-center"
                  >Don't have an account? <a href="/signup">Sign Up</a></small
                >
              </form>
            </section>
          </div>
        </div>
      </main>
    </div>
    <!---->
    <%-// include("../partials/footer") %>
  </body>
</html>
